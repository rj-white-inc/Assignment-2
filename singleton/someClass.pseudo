public class SomeClass {

    public main() {
        /** CHANGE: Get the singleton instance of Logger **/
        Logger l = Logger.getInstance();
        l.info("hello world");
    }
}

public class SomeOtherClass {

    public main() {
        /** CHANGE: Get the singleton instance of Logger **/
        Logger l = Logger.getInstance();
        l.info("hello from some other class!");
    }
}
